interface:
  display_name: "寻找社区帮助"
  short_description: "Find mature external help when an agent is stuck"
  default_prompt: "Use $find-community-help only when an active thread is stuck, progress has stalled, local attempts are looping, the agent may be reinventing a mature solution, or the user explicitly asks for community experience. Generate a redacted advisory query plan with official anchoring plus community cross-validation."

policy:
  allow_implicit_invocation: false
